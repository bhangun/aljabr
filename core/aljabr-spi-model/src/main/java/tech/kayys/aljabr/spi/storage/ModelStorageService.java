package tech.kayys.aljabr.spi.storage;

/**
 * Compatibility bridge for older modules that still reference the pre-move
 * storage SPI package.
 */
public interface ModelStorageService extends tech.kayys.aljabr.spi.model.ModelStorageService {
}
