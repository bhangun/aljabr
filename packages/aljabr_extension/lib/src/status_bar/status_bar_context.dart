import '../context/contribution_context.dart';

class StatusBarContext extends BaseContributionContext {
  const StatusBarContext({
    super.targetId = 'aljabr.status_bar',
    super.data = const {},
  });
}
