import 'package:flutter/material.dart';
import '../../activity_bar/activity_bar_widget.dart';

class ActivityBarSlot extends StatelessWidget {
  const ActivityBarSlot({super.key});

  @override
  Widget build(BuildContext context) {
    return const ActivityBarWidget();
  }
}
